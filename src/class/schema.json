{
  "$schema": "http://json-schema.org/schema",
  "id": "RegridService",
  "title": "Regrid Class Options Schema",
  "description": "Regrid Class",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the class",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What would you like to name this class?"
    },
    "project": {
      "type": "string",
      "description": "Generate in specific Angular CLI workspace project"
    }
  },
  "required": [
    "name"
  ]
}
