{
  "$schema": "http://json-schema.org/schema",
  "id": "RegridService",
  "title": "Regrid Directive Options Schema",
  "description": "Regrid Directive",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the directive",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What would you like to name this directive?"
    },
    "project": {
      "type": "string",
      "description": "Generate in specific Angular CLI workspace project"
    }
  },
  "required": [
    "name"
  ]
}
