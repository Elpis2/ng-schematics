{
  "$schema": "http://json-schema.org/schema",
  "id": "RegridService",
  "title": "Regrid Enum Options Schema",
  "description": "Regrid Enum",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the enum",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What would you like to name this enum?"
    },
    "project": {
      "type": "string",
      "description": "Generate in specific Angular CLI workspace project"
    }
  },
  "required": [
    "name"
  ]
}
